\documentclass[oneside,draft]{amsart}
\usepackage{amsmath,amssymb,amsthm,stmaryrd}
\usepackage[final]{hyperref}
\usepackage[final]{graphicx}
\usepackage{xcolor}
\usepackage{fixme}
\fxsetup{draft}
\fxusetheme{color}

\newcommand{\setcond}[2]{\left\{#1\mid#2\right\}}
\newcommand{\norm}[1]{\lVert #1\rVert}
\newcommand{\diam}{\operatorname{diam}}

\newtheorem{thm}{Theorem}
\newtheorem{lem}{Lemma}
\newtheorem{prop}{Proposition}
\newtheorem{note}{Note}
\newtheorem{obvious}{Obvious}
\newtheorem{cor}{Corollary}
\newtheorem{claim}{Claim}
\newtheorem{rem}{Remark}
\newtheorem{defn}{Definition}
\newtheorem{exer}{Exercise}

\title{A counterexample against Kakeya's conjecture}
\author{Victor Porton, ORCID 0000-0001-7064-7975}
\email{porton.victor@gmail.com}
\subjclass[2020]{51N20, 54H99, 28A78, 54G20, 51M05}
\keywords{Kakeya conjecture, dimensionality, geometry, general topology, long-standing open problem}

\begin{document}

\begin{abstract}
I give a counterexample against Kakeya's conjecture for~$n=2$, signifying that the classic proof was in error.
\end{abstract}

\maketitle  

\section{Intro}

It can be seen from the drawing~\ref{fig:example}, that Kakeya conjecture is false for~$n=2$. Below I will present a rigorous proof of this.

\begin{figure}[hbt]
    \centering
    \includegraphics[width=\textwidth]{cantor2}
    \caption{Counterexample to Kakeya for~$n=2$.\\The blue is the Cantor set, the red is a Besicovitch set}
    \label{fig:example}
\end{figure}

Therefore the classic proof~\cite{kakeya2d} is wrong. Many articles need to be retracted.

\section{The problem}

I will point a counterexample against Kakeya's conjecture for~$n=2$.

\textbf{Kakeya's conjecture for $n=2$.} A union of unit-length intervals on a plane has Hausdorff dimension~$2$, if there are intervals of all possible directions among them.

I will denote Hausdorff dimension by $\dim$.

The counterexample:

Let $x\mapsto u(x)=(x_0,x_1,\dots,)$ be any of (many) mappings from $x\in[0;1]$ to its binary representation, i.e. $x_i\in\{0,1\}$, $u(x)=\sum_{i=1}^\infty \frac{x_i}{2^i}$.

Then define $c(x)=\sum_{i=1}^\infty \frac{2x_i}{3^i}$. It is well known that $c(x)\in C$ where $C$ is the Cantor set.

The counterexample is the union
\begin{equation}\label{maindef}
U=\bigcup_{x\in[0;1[}S_x
\end{equation}
of intervals \[ S_x = \setcond{(c(x)-t\cos(\pi x), t\sin(\pi x))}{t\in\left]0;1\right[}. \]

\section{The proof}

% Following Donald Knuth, I will denote $f\in O(g)$, when asymptotically \[ \exists p_1,p_2>0: p_1 g\leq f\leq p_2 g. \]

% \begin{prop}
% $\setcond{O(y)}{y\in O(x)} = O(x)$.
% \end{prop}

% \begin{proof}
% ~
% \begin{multline*}
% \setcond{O(y)}{x\in O(x)} =\\ \setcond{y}{\exists p_1,p_2,q_1,q_2>0: q_1 p_1 x\leq y\leq q_2 p_2 x\text{ asymptotically}} =\\
% \setcond{y}{\exists p_1,p_2>0: p_1 x\leq y\leq p_2 x\text{ asymptotically}} = O(x).
% \end{multline*}
% \end{proof}

% \begin{prop}
% $a\cdotO(b)=O(a\cdot b)$.
% \end{prop}
            
% \begin{proof}
% ~
% \begin{multline*}
% a\cdotO(b) = a\cdot\setcond{f}{\exists p_1,p_2>0: p_1 b\leq f\leq p_2 b\text{ asymptotically}} = \\
% \setcond{a\cdot f}{\exists p_1,p_2>0: p_1 b\leq f\leq p_2 b\text{ asymptotically}} = \\
% \setcond{f}{\exists p_1,p_2>0: a p_1 b\leq f\leq a p_2 b\text{ asymptotically}} = \\
% \setcond{f}{\exists p_1,p_2>0: p_1 ab\leq f\leq p_2 ab\text{ asymptotically}}=O(a\cdot b).
% \end{multline*}
% \end{proof}

% \begin{cor}
% $O(f)\cdotO(g)=O(f\cdot g)$.
% \end{cor}

% \begin{proof}
% Apply the proposition twice and after this another proposition:
% \begin{multline*}
% O(f)\cdotO(g)=\setcond{a\cdotO(g)}{a\in O(f)} =\\ \setcond{O(a\cdot g)}{a\in O(f)} = \setcond{O(x)}{x\in O(f\cdot g)} = O(f\cdot g).
% \end{multline*}
% \end{proof}

Obviously, each interval~$S_x$ is of length~$1$.

Obviously, $U$~contains intervals of all directions.

Partition $\left[0;1\right[$ into~$2^m$ intervals $I_s=\left[\frac{k}{2^m};\frac{k+1}{2^m}\right[$, $k=0,\ldots,2^m-1$, each of length $1/2^m$, binary digits $s=(s_1,\ldots,s_m)$ (the first~$m$ binary digits of a number). Define $U_s=\bigcup_{x\in I_s} S_x$, so by~\eqref{maindef} we have $U=\bigcup_s U_s$.

For $x\in I_x$, express $c(x)=a_s+r$, where $a_s=\sum_{i=1}^m \frac{2s_i}{3^i}$ and \[r=\sum_{i=m+1}^\infty \frac{2x_i}{3^i}.\] We have $r\leq 1/3^m$ by properties of ternary numbers.
So $c(x)\in[a_s;a_s+1/3^m]$. Approximate~$U_s$ with a parallelogram~$T_s$:
\[
T_s = \bigcup_{c\in[a_s;a_s+1/3^m]} \setcond{(c(x_s)-t\cos(\pi x_s), c(x_s)+t\sin(\pi x_s)}{t\in[0;1]},
\]
where $x_s=k/2^m$. This parallelogram has base $1/3^m$ and height $\lvert\sin(\pi x_s)\rvert$ with area $O(1/3^m)$.

For $x\in I_s$, $\lvert\cos(\pi x)-\cos(\pi x_s)\rvert$ and $\lvert\sin(\pi x)-\sin(\pi x_s)\rvert$ are (mean value theorem) $O(1/3^m)$, so $U_s$ lies within a $\delta$-neigh\-bor\-hood of~$T_s$, with $\delta\in O(1/3^m)$, because:
\begin{multline*}
\lvert(c(x_s)-t\cos(\pi x_s), c+t\sin(\pi x_s) - ((c(x)-t\cos(\pi x), t\sin(\pi x)))\rvert=\\
\lvert((c(x_s)-c(x))-t(\cos(\pi x)-\cos(\pi x_s), t(\sin(\pi x_s)-\sin(\pi x)))\rvert\in O(1/3^m),
\end{multline*}
because $c(x_s)-c(x)\in O(1/3^m)$ and $\lvert\cos(\pi x)-\cos(\pi x_s)\rvert$ and $\lvert\sin(\pi x)-\sin(\pi x_s)\rvert$ are $O(1/2^m)$,
$\lvert t\rvert\leq 1$.

Further, split $T_s$ into smaller parallelograms:
\[
T_{sl} = \bigcup_{c\in[a_s;a_s+1/3^m]} \setcond{(c(x_s)-t\cos(\pi x_s), c(x_s)+t\sin(\pi x_s))}{t\in\left[\frac{l}{2^m};\frac{l+1}{2^m}\right[},
\]
where $l=0,\ldots,2^m-1$.

Let fix balls of the radius $\epsilon = 1/3^m$.

Cover $T_{sl}$ with balls.
Since $T_{sl}$ lies inside a rectangle of dimensions \( 1/3^m \times 1/2^m \), we can cover it with
\[
O(1) \cdot O(3^m / 2^m) = O(3^m / 2^m)
\]
balls.

Therefore $T_s$~can be covered by $O(2^m)\cdot O(3^m / 2^m) = O(3^m)$ and thus $U_s$ (as $O(3^m)$-neigh\-bor\-hood of~$T_s$) can also be covered by balls of radius $2\epsilon$ where $\epsilon\in O(3^m)$, that is by $O(3^m)$~balls of radius~$\epsilon$.

For \( U \), with \( 2^m \) sets \( U_s \), use:
\[
2^m \cdot O(3^m) = O(2^m \cdot 3^m)
\]
balls.

The $d$-di\-men\-sio\-nal Hausdorff measure sum is:
\[
O(2^m \cdot 3^m) \cdot O(1/3^m)^d = O(2^m\cdot 3^m\cdot 3^{-md}) = O(2^m\cdot 3^{m(1-d)}).
\]

Obviously, $2^m\cdot 3^{m(1-d)}=2^m\cdot 3^{m(-1-\epsilon)}\to 0$ for $d=2-\epsilon$ for small epsilons.

So the Hausdorff measure $H^{2-\epsilon}(U)=0$. Therefore \[ \dim U=\inf\setcond{d\geq 0}{H^d(U)=0}\leq 2-\epsilon. \]

\section{``Political'' context}

I discovered ordered semigroup/semicategory actions in 2019. The world didn't react obeying like a sheep to decision of Russian Orthodoxes to kick people like me from normal life such as academic career. That's scary: the most valuable component is a missing component. Missing ordered semigroup actions amount to like half of world economy missing~\cite{osa-important}.

I don't know how Kakeya conjecture is used in the rest of math, but I use it to build a bridge between two about halves: academic math and the second lost half, through my glorification.

The advice: ``Solve some famous open problem to prove that you are a good mathematician.'' sounded for me like a mocking. But in the new reality, I indeed did it.

\bibliographystyle{amsplain}
\bibliography{kakeya}

\end{document}
